Python 3.12 / FastAPI + Uvicorn; Pydantic v2 Settings (`pydantic_settings.BaseSettings`); SQLAlchemy async + Alembic; Redis 7 (cache + Celery broker/result backend); Celery workers; NudeNet v3.4.2 (ONNX Runtime), OpenAI CLIP (`openai/clip-vit-base-patch32`), Ultralytics YOLOv8 Nano (`yolov8n.pt`), facenet-pytorch MTCNN, PaddleOCR + `better-profanity`; JWT via `python-jose` HS256; OAuth2 scaffolding for Google/GitHub; Prometheus client mounted at `/metrics`; Docker Compose for local orchestration.